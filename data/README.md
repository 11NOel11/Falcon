# Data Directory

This directory will contain the CIFAR-10 dataset.

The dataset will be automatically downloaded when you run the training script for the first time:

```bash
python train.py --optimizer falcon_v5 --epochs 10
```

The CIFAR-10 dataset (~170MB) will be downloaded from the official source.
